# kakapo AI 워커

해커톤 프로젝트. 커플의 채팅을 실시간 분석해 개입을 산출하는 **kakapo**의 AI 워커 파트.

## 이 레포의 범위

**이 레포는 AI 워커만 담당한다.** 채팅 서버(Spring Boot)는 팀원이 별도 레포에서 개발 중이다.

- ✅ 이 레포: 대화 분석 → 개입 판정 → 위젯 페이로드 생성
- ❌ 이 레포 아님: 채팅 API, 실시간 전송, 프론트엔드, 인증, 배포 인프라

## 팀

| 역할 | 담당 |
| --- | --- |
| PM | 영환 |
| 디자인 | 혜원 |
| 프론트엔드 | 민상 |
| 채팅 서버 (Spring Boot) | 팀원 |
| AI 워커 (이 레포) | 나 |

## 문서 지도

| 문서 | 내용 |
| --- | --- |
| `CLAUDE.md` | 이 문서 — 프로젝트 상시 맥락 |
**명세·설계 — 평소엔 안 열고 뭔가 이상할 때 연다.**

| 문서 | 내용 | 누구 것 |
| --- | --- | --- |
| `docs/contract.md` | **백엔드 연동 규격** (임의로 못 바꿈) | 채팅 서버 담당자 |
| `docs/spec.md` | PM 기능 명세 3종 (위젯 ②번 줄) + 구현 판단 기록 | PM |
| `docs/design.md` **1부** | 대화 분절(세그먼테이션) 설계 + 실측 근거 | 우리 |
| `docs/design.md` **2부** | **실 상태 표현** — 위젯 ①번 줄 (상시) 설계 | 우리 |
| `docs/design.md` **3부** | 기억/RAG 평가셋 — 외부 멀티세션 대화 전처리 | 우리 |

**앞의 둘은 남의 문서다.** 우리 판단을 검증하는 기준점이라 내용을 고치지 않는다.

**전달본·운영 — 버전을 안 붙인다.** 같은 파일을 갱신한다.

| 문서 | 내용 | 받는 사람 |
| --- | --- | --- |
| `docs/server-handoff.md` | **연동 전부** — 엔드포인트·요청/응답·감정 분석 필드·협의 기록 | 서버 담당자 |
| `docs/worker-tasks.md` | 작업 지시서 / 진행 상황 | 나 |
| `docs/demo-checklist.md` | **시연 전 확인 목록** — 코드 프리즈 2026-08-21 10:00 | 나 |
| `docs/demo.md` | **시연 진행 대본** — 본번에 이것만 보고 위에서 아래로 진행 | 나 |
| `docs/tech-qa.md` | **기술 QA 대비** — 예상 질문·즉답 숫자·약점 | 나 |
| `docs/QA.md` | **배포 QA 기록** — 실사용 시나리오 실측·발견 사항 (2026-08-20) | 나 |
| `docs/refactoring.md` | **레이턴시 리팩토링 기록** (2026-09-13) — 무엇을 왜 바꿨고 무엇을 안 바꿨나, 전후 실측 | 나 |
| `README.md` | 코드 설명서 | 나 |

프론트(민상) 전달분은 아직 별도 파일이 없다 — `design.md` 2부 13장에 있다.
분량이 늘면 `frontend-handoff.md` 로 떼어낸다.
| `parked/README.md` | 파킹된 대화 소재 기능과 복구 방법 |
| `README.md` | 코드 설명서 |

---

## 서비스 개요

카카오톡 같은 채팅방 **1개**. 커플 두 명(A, B)이 대화한다.

```
┌─────────────────────────────┐
│  ① 서운해 보여요             │  ← 실 상태 표현. 상시. 비지 않는다
│  ② 카카포가 추천하는 …        │  ← 3종 개입. 미발동이면 빈 줄
├─────────────────────────────┤
│  A: 오늘 뭐했어?             │
│  B: 그냥 집에 있었어          │  ← 커플만의 공간
│  A: 아 그렇구나              │     봇 메시지 삽입 금지
├─────────────────────────────┤
│  [입력창]                    │
└─────────────────────────────┘
```

**위젯은 두 줄이고, 응답의 배열도 두 개다.** ① = `emotionAnalyses`, ② = `results`.
두 줄은 자리를 다투지 않는다 — 3종이 전부 미발동이어도 ②만 비고 ①은 남는다.
그래서 **②의 빈 줄은 정상이고 ①의 빈 줄은 사고다.** 설계는 `docs/design.md` 2부.

**핵심: 위젯은 사람마다 다르게 보인다.** 같은 방이지만 상단만 개인화된다.

| | A 화면 | B 화면 |
| --- | --- | --- |
| 공통 결과 (`COUPLE`) | 보임 | 보임 (동일 내용) |
| 개별 결과 (`INDIVIDUAL`, 대상 A) | 보임 | **안 보임** |
| 대화 스트림 | 동일 | 동일 |

B에게 개별 코멘트가 갔을 때 A는 그런 게 떴다는 사실 자체를 모른다. 이게 의도된 설계다 —
지적성 피드백이 상대에게 노출되면 그 자체가 갈등 소재가 된다.

서버는 `visibilityType`과 `targetParticipant`를 보고 **수신자별로 갈라서** 전송해야 한다.
방 단위 브로드캐스트만 하면 개별 결과가 양쪽에 다 뜨고, 이 설계가 통째로 깨진다.

> ⚠️ **②번 줄에 3종이 동시 발동하면 몇 개까지 뜨는지 미확정.** 한 줄이면 하나만 뜨고,
> 그러면 **워커의 우선순위 순서가 곧 화면 선택**이 된다(`results[0]`). 워커는 규격대로
> 전부 실어 보내고 우선순위 순으로 정렬해 둔다. 프론트(민상) 확인 대기 중.

---

## 절대 제약 (이 프로젝트의 핵심 가치)

**관계 상태를 언급하는 문구를 절대 출력하지 않는다.**

금지 예시:
- "요즘 대화가 줄었어요"
- "권태기 같아요"
- "두 분 사이가 서먹해 보여요"
- "대화가 뜸해지셨네요"

이유: 누군가에게 "너네 권태기다"라는 말을 들으면 의식이 심해지고, 평소 하지 않던 생각까지
하게 되어 오히려 관계가 악화된다.

**내가 눈치채기 전에 kakapo가 먼저 눈치채고 조용히 회복시킨다** — 이게 서비스의 핵심 가치다.

따라서 **감지 로직과 발화 로직은 완전히 분리된다.** 트리거는 갈등·권태를 감지하지만,
출력은 그것을 절대 드러내지 않는다. 이 분리가 이 프로젝트 설계 난이도의 대부분을 차지한다.

프롬프트를 수정하거나 새 출력 문구를 만들 때 이 제약을 반드시 확인할 것.

### 이 제약이 걸리는 범위 — 헷갈리기 쉬움

금지 대상은 **관계 상태를 규정하는 말**이다. 감정 상태를 언급하는 것 전부가 아니다.

| | 예시 | |
| --- | --- | --- |
| ❌ 관계 규정 | "요즘 대화가 줄었어요", "권태기", "사이가 서먹" | 모든 기능에서 금지 |
| ✅ 감정 알아봄 | "지금 감정이 조금 올라와 있는 것 같아요" | **말투 교정에서는 오히려 이게 기능이다** |

다만 말투 교정에서도 **사람 자체를 평가하지는 않는다.** "무례하시네요"는 안 되고,
표현을 두고 "공격적으로 들릴 수 있어요"는 된다. **대상이 사람이면 안 되고 말이면 된다.**
이건 하드 필터가 아니라 프롬프트에서 다룬다 — 단어 목록으로는 두 경우를 구분할 수 없다.

### 외부에서 가져온 문자열도 검사한다

유튜브 영상 제목처럼 **우리가 쓰지 않은 문자열도 화면에는 그대로 뜬다.** "권태기 극복법"이라는
제목이 위젯에 박히면 절대 제약이 그대로 깨진다. 그래서 `filter.py`는 우리 생성물뿐 아니라
영상 제목·설명까지 검사하고, 유튜브 검색어에 '권태기'를 쓰지 못하게 프롬프트로 막는다.

---

## 아키텍처

```
백엔드 요청 (docs/contract.md 5장)
   ↓
pipeline.analyze()          AnalysisRequest 파싱 · 검증
   ↓
router.run()                **전부 여기서** — 독립인 것은 동시에 (docs/refactoring.md)
   │
   ├ 말투 판정 → 말투 생성   분절을 **안 기다리고** 먼저 출발 (입력이 분절과 무관 — 마지막 4개)
   │                        분절 뒤 게이트를 활성 세그먼트로 재계산해 같으면 채택, 다르면 재실행
   ├ router.split()          대화 분절 — 스트림을 화제 단위로 끊는다
   │                        ① 룰 컷: 3시간 이상 공백  ② LLM: 마지막 조각만 화제로
   │                        **점수 캐시** — 같은 방의 같은 발화는 다시 채점하지 않는다
   │                        이후 단계는 전부 **활성 세그먼트(마지막)** 만 본다
   │   ├ read_state()        실 상태 — 위젯 ①번 줄, 게이트 없이 매 요청
   │   ├ 데이트 계획 → 카카오 → 데이트 문구
   │   ├ 유튜브 (말투 결과를 본 뒤)
   │   └ harvest_memories()  기억 추출 → 저장. **백그라운드** — 응답을 기다리게 하지 않는다
   ↓                        후보 결과는 우선순위 순으로 정렬 — 위젯 ②번 줄
   │
   ├─ 1. 갈등 중재 (TONE_CORRECTION)
   │     check_tone_gate()   룰 — 공격·오해 유발 표현 6종, 개인 기준선 대비
   │     tone_judge()        LLM — 진짜 갈등인가, 맥락상 장난인가
   │     tone_suggest()      LLM — 진단 + 대체 문장 + 이유
   │
   ├─ 2. 데이트 코스 (DATE_RECOMMENDATION)
   │     check_date_gate()   룰 — 데이트 의도 4종
   │     retrieve_many()     RAG — 대화 맥락과 유사한 기억
   │     plan_date()         LLM — 무엇을 검색할지 (장소를 지어내지 않는다)
   │     search_places()     카카오 로컬 API — 실재하는 장소
   │     write_reason()      LLM — 코스명·요약·추천 이유
   │
   └─ 3. 유튜브 영상 (YOUTUBE_RECOMMENDATION)
         check_concern_gate() 룰 프리필터 — 고민 신호가 아예 없으면 LLM 안 부름
         classify_concern()   LLM — 고민 유형 + 검색어
         find_candidates()    YouTube API — 영상 + 베스트 댓글 3개
         pick_video()         LLM — 댓글까지 읽고 1개 선정, 없으면 침묵
   ↓
filter()      금지어 하드 필터 (외부 문자열 포함)
   ↓
AnalysisResponse (COMPLETED / SKIPPED / FAILED)
```

**LLM 과 외부 API 의 역할이 엄격히 갈린다.** 이게 이 설계의 핵심이다.

| LLM | 외부 API |
| --- | --- |
| 무엇을 찾을지, 왜 이것인지 | 실제로 무엇이 있는지 |
| 검색어·지역·코스 의도·추천 이유 | 상호명·카테고리·URL·영상 ID·썸네일 |

LLM 이 상호명이나 영상 ID 를 만들면 **존재하지 않는 가게와 죽은 링크**가 화면에 뜬다.
규격서에서 `externalUrl` · `videoId` · `thumbnailUrl` 이 전부 필수 필드라 환각이 그대로 나간다.
**그래서 이름과 링크는 외부 API 가 준 것만 쓴다.** 이 원칙을 깨는 코드를 쓰지 말 것.

### 후속 — 서버 연결

```
[채팅 서버] ── POST /internal/v1/chat-analyses ──▶ [이 레포: AI 워커]
             ◀── AnalysisResponse (동기) ─────────
```

**HTTP 서버는 `worker/api.py` 다** (FastAPI, 2026-08-18). `analyze()` 를 그대로 물리고
**판정 로직을 하나도 갖지 않는다** — CLI(`tools/run.py`)·devui 와 같은 함수를 부른다.
붙이는 방법은 `docs/server-handoff.md`(서버 담당자 전달본).

```
POST /internal/v1/chat-analyses   GET /health   GET /docs · /openapi.json
```

여기서 정한 것 셋. **바꾸려면 서버 담당자와 협의한다.**

| | | 왜 |
| --- | --- | --- |
| 오류도 **HTTP 200** | `FAILED` 봉투로 내보낸다 | 규격서에 상태 코드 규정이 없고 `status`·`errorCode` 로 이미 표현된다 |
| 직렬화는 `to_json_dict()` | FastAPI 기본 직렬화를 안 쓴다 | 기본값은 `exclude_none` 이 안 걸려 `null` 이 나가고 규격서 14장이 깨진다 |
| **듀얼스택 소켓** | `0.0.0.0` 도 `::` 도 아니다 | Railway 사설망은 IPv6 전용, `--host ::` 는 IPv6 전용 소켓이 된다 (둘 다 실측) |

---

## 기술 스택

- **Python 3.11+** (`.venv` 사용 — `.venv/bin/python`)
- **LangChain 1.x** — `langchain`, `langchain-openai`
- **RAG** — `OpenAIEmbeddings` + `InMemoryVectorStore` (기억 검색)
- **numpy** — `InMemoryVectorStore`의 cosine similarity 계산에 필요
- **httpx** — 카카오 로컬 API, YouTube Data API

LangChain을 import하는 파일은 `worker/llm.py`와 `worker/retrieve.py` 둘뿐이다.
나머지는 전부 순수 파이썬이다. LangChain은 OpenAI를 부르는 어댑터로만 쓰고,
판정 품질을 만드는 로직(게이트·필터·라우터)은 직접 짠 코드다.

### 외부 API 키

`.env` 에 넣는다. **없으면 해당 기능만 조용히 미발동하고 워커는 죽지 않는다.**

| 키 | 기능 | 없으면 |
| --- | --- | --- |
| `OPENAI_API_KEY` | 전부 | 동작 불가 |
| `KAKAO_REST_API_KEY` | 데이트 코스 | 미발동 (장소를 지어내지 않는다) |
| `YOUTUBE_API_KEY` | 유튜브 추천 | **시드 폴백** — `data/yt_seed.json` (아래) |

유튜브 무료 쿼터는 하루 10,000 units 이고 `search.list` 가 호출당 100 units 다.
추천 1건에 약 105 units → **하루 약 95회.** 개발 중 반복 실행에 주의.
운영 중 억제는 세그먼트 기준("같은 화제엔 하나만") + `router.YOUTUBE_COOLDOWN_MIN`
백스톱이 맡는다.

**쿼터가 소진되면 시드 폴백으로 넘어간다** (2026-08-19). `data/yt_seed.json` 은
빌드 타임에 **실제 YouTube API 로 수집**한 고민 유형별 영상 19건(댓글 포함, 금지어
검수 완료, `tools/build_yt_seed.py`)이다. 검색이 죽으면(쿼터·키 없음·네트워크) 분류된
고민 유형의 시드에서 `pick_video` 가 평소처럼 댓글을 읽고 고른다 — 지어내는 게 아니라
API 산출물을 미리 받아둔 것이라 "실재하는 것만" 원칙 그대로다. 시드 갱신은 빌드
스크립트를 다시 돌려 커밋한다.

### LangChain 규칙 (중요)

이 프로젝트는 LangChain 1.x를 쓴다. 0.x 시절 API는 전부 폐기됐다.

- ❌ `langchain-classic` 설치 금지
- ❌ `LLMChain`, `initialize_agent`, `AgentType`, `ConversationBufferMemory`, `LLMRouterChain` 등 레거시 API 금지
- ❌ `create_agent` 미사용 — judge는 툴 루프가 없는 단발 분류 호출이다
- ❌ `import openai` 직접 호출 금지. 모든 LLM 호출은 `worker/llm.py` 를 거친다
- ✅ `init_chat_model` + `with_structured_output(Schema, method="json_schema", strict=True)`
- ⏸️ `langgraph`는 MVP에서 쓰지 않는다. `router.py`의 우선순위 체인으로 충분하다

인터넷에서 찾은 LangChain 예제 대부분은 0.x 기준이다. 참고 전에 위 목록에 걸리는지 확인할 것.

### MVP 범위

| | 지금 | 후속 |
| --- | --- | --- |
| 입력 | `fixtures/*.json` + **HTTP POST** (`worker/api.py`) | — |
| 기억 저장 | `data/memories.json` | Postgres |
| 출력 | 콘솔 / `--json` / `devui` / **HTTP 응답** | — |
| 배선 | 함수 호출 + `router.py` | — |
| 배포 | `Dockerfile` + `railway.json` (Railway) | — |

`devui/server.py` 는 브라우저에서 확인하는 용도다. **CLI 와 같은 진입점**(`analyze()`)을
부르고 판정 로직을 갖지 않는다. 표준 라이브러리만 쓰므로 워커 의존성이 늘지 않는다.
위젯이 사람마다 다르게 보이는 것(`A 화면` / `B 화면`), 단계별 시간·토큰, 세그먼트가 왜
거기서 잘렸는지를 본다.

Redis·Postgres·LangGraph는 **판정 품질에 기여하지 않는다.** 지금 단계에서 구현하지 않는다.
필요해 보여도 사용자에게 먼저 확인할 것.

---

## 폴더 구조

```
AI-Worker/
├── CLAUDE.md
├── README.md
├── Dockerfile · railway.json    ← 배포 (Railway). 런타임에 필요한 것만 담는다
├── docs/
│   ├── contract.md              ← 백엔드 연동 규격 (남의 문서)
│   ├── spec.md                  ← PM 기능 명세 3종 (남의 문서)
│   ├── design.md                ← 설계 3부 (분절 · 실 상태 표현 · 평가셋)
│   ├── server-handoff.md        ← 서버 전달본 (연동·필드 정의·협의 기록)
│   ├── worker-tasks.md          ← 구현 기록 (완료)
│   ├── demo-checklist.md        ← 시연 전 확인 목록 (코드 프리즈 8/21 10:00)
│   ├── demo.md                  ← 시연 진행 대본 (한 흐름, 본번용)
│   ├── tech-qa.md               ← 기술 QA 대비 (예상 질문·숫자·약점)
│   └── QA.md                    ← 배포 QA 기록 (2026-08-20 실측)
├── parked/                      ← 대화 소재 기능 아카이브 (import 되지 않음)
├── .env.example
├── requirements.txt
├── fixtures/                    ← case1~case20, 규격서 요청 형식
├── data/
│   ├── memories.json            ← 기억 시드 27건
│   ├── speaker_profiles.json    ← 개인 말투 기준선 시드 (A·B)
│   └── eval/msd_sample.jsonl    ← 평가셋 2000건 (원본 parquet 만 git 제외)
├── devui/                       ← 브라우저 확인용 UI (판정 로직 없음, 의존성 없음)
├── tools/
│   ├── run.py                   ← CLI 러너
│   └── build_eval_set.py        ← 외부 대화 데이터 → 평가셋
└── worker/
    ├── api.py                   ← HTTP 서버 (FastAPI). 판정 로직 없음
    ├── models.py                ← 규격서 DTO + 내부 스키마
    ├── llm.py                   ← LLM 접근 레이어
    ├── text.py                  ← 한국어 문장 판별 유틸
    ├── copy.py                  ← 화면 고정 문구 (PM·디자인 소유)
    ├── router.py                ← 후보 기능 실행 + results 배열
    ├── pipeline.py              ← 요청 파싱 → 응답 생성
    ├── filter.py                ← 금지어 하드 필터
    ├── limits.py                ← 화면 글자 수 한도 (검사 → 재생성 → 절단)
    ├── segment.py               ← 대화 분절 (모든 단계보다 먼저)
    ├── state.py                 ← 실 상태 표현 (위젯 ①번 줄, 상시)
    ├── extract.py               ← 기억 추출 (모든 후보가 공유)
    ├── retrieve.py              ← RAG 기억 검색
    │
    ├── tone.py / profile.py     ← [1] 갈등 중재
    ├── date_course.py / places.py  ← [2] 데이트 코스 (+ 카카오)
    ├── youtube.py / ytapi.py    ← [3] 유튜브 (+ YouTube API)
    │
    └── prompts/
        ├── segment.md
        ├── state.md
        ├── extract.md
        ├── tone_judge.md / tone_suggest.md
        ├── date_plan.md / date_reason.md
        ├── yt_concern.md / yt_pick.md       ← 유튜브 고민 갈래 (유일하게 활성)
        └── yt_topic.md / yt_pick_topic.md   ← 유튜브 화제 갈래 ⛔ 비활성 (라우팅에서 끊음)
```

---

## 대화 분절 (세그먼테이션)

설계와 실측 근거 전문은 `docs/design.md` 1부. 요지만:

**서버는 최근 N개를 통째로 보내고 화제 경계를 모른다.** 그대로 두면 두 시간 전에 끝난
데이트 얘기가 지금 막 싸우기 시작한 요청에서 데이트 코스를 발동시킨다. 실측으로 확인한
고장이고, `fixtures/case11_mixed.json` 이 이 케이스다.

```
① 룰 컷    3시간 이상 공백에서 자른다 (LLM 에게 묻지 않는다)
② LLM 분절  마지막 조각만 화제로 나눈다 (요청당 1회)
③ 라우팅    활성 세그먼트(= 마지막)에만 건다
```

**경계 신호로 쓸 수 있는 룰은 시간 공백 하나뿐이다.** 화제 전환 표지어·어휘 겹침·임베딩
거리를 전부 재봤고 셋 다 무너졌다 (문서 3-1). 새로 룰을 추가하려면 먼저 재볼 것.

**룰과 LLM 이 서로 다른 구멍을 메운다.** LLM 은 화제는 보는데 시간을 못 본다 —
`case4_routine`(사흘치)을 한 덩어리로 본다. 룰 컷이 그걸 메운다.

**말투(`tone_score`)로 단독 절단하지 않는다.** 회색지대의 보조 신호로만 쓴다.
`case7_tone` 은 호칭이 "오빠 → 야"로 바뀌지만 처음부터 끝까지 저녁 약속 얘기 하나다.
여기서 끊으면 말투 판정이 "왜 화가 났는지"(야근으로 약속이 깨짐)를 못 보고 대체 문장이
일반론이 된다 — 말투 교정이 자기 발밑을 판다.

**같은 발화를 요청마다 다시 채점하지 않는다 — 점수 캐시** (2026-09-13, `docs/refactoring.md`).
백엔드가 메시지 1건마다 창 30개를 통째로 보내는데 새 발화는 한두 개다. 발화의 점수는
"직전 맥락과 얼마나 이어지는가"라 (방, 발화 id, 직전·이번 내용 지문)이 같으면 재료가
같다. 캐시에 없는 꼬리만 그 앞 전체를 '앞 맥락'으로 붙여 채점한다 — 창 30개 3~4초가
새 발화 채점 ~1초가 된다. **상태가 아니라 메모다** — 비어 있으면 전부 다시 채점하고
규칙은 안 바뀐다. 부수 효과로 경계가 요청마다 안 흔들려 "같은 화제엔 하나만" 억제가
안정된다. `KAKAPO_SEGMENT_CACHE=0` 으로 끈다.

> ⚠️ **`SCORE_BATCH` 를 15 아래로 내리지 말 것.** 10 으로 재보니 case19·case21 에서
> 경계를 잃었다 (3/3). 채점 대상이 짧으면 모델이 낮은 점수를 안 준다.

**맥락과 트리거를 갈라 쓴다.**

| | 보는 범위 |
| --- | --- |
| 게이트 · 트리거 id · RAG · 기억 추출 | `ctx.active` (활성 세그먼트만) |
| 말투 판정 프롬프트의 직전 대화 | `ctx.context` (부족분만 앞에서 채움) |
| 실 상태 표현 (`state.py`) | `ctx.context` — 추이(쌓임→풀어짐)에 두 시점이 필요하다 |
| 말투 기준선 (`profile.py`) | `ctx.messages` (전체 — 표본이 넓을수록 정확) |

> ⚠️ **화제 라벨을 만들지 않는다.** 경계를 룰이 나중에 정하므로 채점 시점의 LLM 은 세그먼트
> 범위를 모른다. 억지로 라벨을 받으면 "권태기 조짐" 같은 문자열이 생기는데 `filter.py` 는
> `AiResult` 의 화면 문자열만 검사해서 안 걸러진다. **안 만드는 게 가장 확실한 방어다.**
> 디버깅은 점수와 `note` 가 대신하고, 그것도 트레이스 밖으로 나가지 않는다.

---

## 실 상태 표현 — 위젯 ①번 줄 (상시)

설계 전문은 `docs/design.md` 2부. 요지만:

**후보 기능이 아니다.** 게이트가 없고 매 요청 돌며, 결과가 `results` 가 아니라
`emotionAnalyses`(규격서 11장)로 나간다. **규격서가 비워 둔 자리를 그대로 쓴다 —
연동 규격 변경이 없다.**

**둘의 상황 하나를 두 화면에 똑같이 띄운다** (PM 기획 변경 2026-08-19 — 원래는 각자
상대방의 상태를 보는 개인화였다). 화자별 4축 점수를 **축별 최댓값**으로 병합해 한 번
판정한다 — 평균이면 한쪽의 분노가 상대의 평온에 희석돼 갈등 신호가 사라진다.
**규격은 안 바뀌었다.** `subject`/`viewer` 두 항목을 그대로 내보내되 라벨·강도·문구가
동일하다. 서버 분배·프론트 렌더링 무변경. ①번 줄 문구는 여전히 감정 알아봄이지 관계
규정("사이가 서먹")이 아니다 — 절대 제약 경계는 그대로다.

| 상태 라벨 | 감정 | 실 모양 · 글로우 | 문구 |
| --- | --- | --- | --- |
| `STABLE` | 평온 / 기본 | 축 늘어진 곡선 · 무채색 | 평온해요 |
| `RESOLVED` | 애정 / 설렘 (풀어짐) | 하트 매듭 · 핑크 | 다정해 보여요 |
| `ACCUMULATED` | 서운함 / 오해 (쌓임) | 엉킨 매듭 · 파랑 | 서운해 보여요 |
| `ENGAGED` | 들뜸 / 활기 | 큰 파동 · 초록 | 신나 보여요 |
| `ESCALATED` | 분노 / 격앙 | 날카로운 스파이크 · 빨강 | 감정이 올라와요 |

**LLM 은 감정 4축 점수(애정·서운·활기·분노, 각 0~5)까지만 낸다.**
라벨은 `state.pick_label()` 의 임계값이 정하고, 문구는 `copy.STATE_TEXT` 사전에서 고른다.
분절이 "LLM 은 점수만, 경계는 룰이"인 것, 데이트가 "LLM 은 검색어만, 상호명은 카카오가"인
것과 같은 패턴이다.

**라벨을 LLM 이 고르게 했다가 실측에서 깨져 바꿨다.** `case11_mixed` 에서 말투 교정은
공격 표현으로 잡은 발화를 상태 산출은 서운함으로 읽었다 — 한 발화를 두 기능이 다르게
읽었고 조정할 손잡이가 없었다. 지금은 손잡이가 다섯 개다.

| 상수 | 값 | 뜻 |
| --- | --- | --- |
| `MIN_SCORE` | 3 | 최고점이 이 아래면 `STABLE` (프롬프트 앵커 "3 = 뚜렷하다"에 맞춤) |
| `ANGER_WINS` | 3 | 분노가 이 이상이면 다른 축보다 낮아도 `ESCALATED` |
| `PRIORITY` | 분노→서운→활기→애정 | 동점일 때의 순서 |
| `AFFECTION_WORDS` | 사랑·보고싶… | 직전 발화에 있으면 애정 축을 `MIN_SCORE` 까지 **바닥값 보정** |
| `SCORE_RECENT` | 6 | **채점은 마지막 이 개수만.** 앞은 "채점 금지" 구획으로 흐름만 보여준다 |

**`SCORE_RECENT` 는 격앙 고착을 막는다** (2026-08-20 배포 QA8 실측 — 싸움 뒤 화해 6건
+ 화제 전환 4건에도 `ESCALATED` 가 안 내려왔다). 싸움 발화가 인용 가능한 로그에 있는 한
"3 = 문장으로 짚을 수 있다" 앵커에 분노가 계속 걸려서, 분절 채점처럼 구획을 갈라 인용
자체를 막았다. 발화 유무·애정 낱말 판정도 채점 구간 기준이다.
`fixtures/case19_washout.json` 이 이 케이스다.

**애정 룰은 바닥값이지 강제 라벨이 아니다** (2026-08-19 연동 실측 — "사랑해"가 `STABLE`
로 떴다). 보정 뒤 판정은 그대로 `pick_label()` 이 해서, "사랑 같은 소리 하네" 같은
반어는 분노 점수(`ANGER_WINS`)가 막는다. `fixtures/case18_affection.json` 이 이 케이스다.

**`STABLE` 에 해당하는 축은 없다.** `calm` 축을 뒀더니 모델이 바닥값으로 깔아서 다른 축을
전부 먹었다. 평온은 다른 감정이 없는 상태지 경쟁하는 감정이 아니다.

화면 문구를 생성시키지 않는 이유는 따로 있다 — 상시 노출이라 자유 생성은 절대 제약 위반
확률을 매 요청 곱한다.

**애매하면 `STABLE` 이다.** 근거 없이 감정을 지어내면 없던 갈등을 만든다.
화자 단위 안전 규칙(발화 없음·미채점·confident 아님 → 기여 0)은 병합 **전에** 그대로
적용된다. 커플 병합 뒤에는 한쪽 발화만으로도 둘의 상황이 갱신되므로 `shouldShow` 는
항상 `true` 다 (LLM 호출 실패 시에만 빈 배열 = 갱신 없음).

**추이는 라벨 안에 있다.** `RESOLVED`(풀어짐)·`ACCUMULATED`(쌓임)·`ESCALATED`(격해짐)가
그것이다. 별도의 trend 축을 만들지 않는다.

> ⚠️ **`filter.py` 의 `visible_texts()` 는 `AiResult` 만 본다.** 상태 문구는
> `banned_in_state()` 로 따로 검사한다. 세그먼테이션에서 화제 라벨을 안 만든 것과 같은
> 함정이라 새 필드를 화면에 내보낼 때마다 이 경로를 확인할 것.

> ⚠️ **`SKIPPED` 가 사실상 사라졌다.** `results` 와 `emotionAnalyses` 가 둘 다 비어야
> `SKIPPED` 인데 ①번 줄이 상시라 거의 `COMPLETED` 로 나간다. 서버가 `SKIPPED` 를
> "아무것도 안 함"으로 보고 전송을 건너뛰면 ①번 줄이 영영 안 뜬다.

---

## 후보 기능 3종 — 위젯 ②번 줄

기능 명세 전문은 `docs/spec.md`, 출력 규격은 `docs/contract.md` 를 본다.
**세 후보 모두 활성 세그먼트만 본다** (위 참조). 셋 다 미발동이면 ②번 줄은 그냥 빈다.

### 1. 갈등 중재 — 말투 교정 (`TONE_CORRECTION`)

감정이 격양되어 공격적·오해 유발 표현을 보낸 사람에게 **그 사람에게만** 제시한다.
**방금 전송된 메시지 하나**를 본다.

트리거 6종: `insult` / `generalization` / `sarcasm` / `address_change` / `repetition` / `abrupt_change`

`abrupt_change`는 **약한 증거다.** 다른 신호와 함께일 때만 세고, 혼자서는 하위 신호
`ABRUPT_ALONE_SIGNALS = 2` 개 이상일 때만 인정한다.

> ⚠️ **상관된 신호를 따로 세지 않는다.** 짧아진 메시지에는 ㅋ 도 이모지도 당연히 없다 —
> 셋을 각각 세면 사실 하나를 세 번 세는 것이라 임계값이 그냥 찬다. 실측에서
> **"홍대도 좋아"(5자)가 신호 3개로 발동**해 교정 카드가 떴다. 지금은 짧아짐이 잡히면
> ㅋ·이모지 부재를 세지 않고, 그래서 임계값도 3 → 2 로 내렸다.
>
> **짧은 동의·긍정 응답은 짧아짐으로 세지 않는다.** 명세의 `abrupt_change` 는 "맥락 없는
> 단답"인데 "홍대도 좋아"는 대화가 굴러가는 신호다. 길이만으로는 둘이 구분되지 않는다.
> 마침표 종결("됐어.")은 그대로 센다. `fixtures/case12_agree.json` 이 이 케이스다.
>
> **ㅋ 부재와 이모지 부재도 하나의 신호다** (2026-08-20 실측). 진지한 메시지엔 둘 다
> 없는 게 당연한데 따로 세니 ㅋ·이모지를 즐겨 쓰는 기준선에서 **모든 평범한 진지
> 메시지가 신호 2개**로 게이트를 지나 판정 LLM 편차에 노출됐다 — "점심은 집에 있는거
> 먹었어"에 교정 카드가 떴다. 부작용으로 약한 답답함만 있는 발화(case15 마지막 줄,
> case10 "답답해")는 이제 게이트에서 침묵한다 — **과잉 개입 3건(실측)과 맞바꾼 의도된
> 보수화**고, 명백한 도발은 어휘("장난해?" 추가)가 되잡는다.
>
> **반복 트리거는 "밀어붙임"만 잡는다** (2026-08-20). 예전 규칙(교집합 + 15자 이하)은
> 흔한 단어를 공유한 잡담("집에 있어" / "집에 있는거 먹었어")을 잡고 정작 "전화 받아 -
> 받아 - 받으라고"는 교집합이 비어 못 잡는, 의도와 반대인 모양이었다. 지금은 인접한
> 두 짧은 메시지에서 **공유 토큰이 양쪽 절반 이상**일 때만 인정한다.

**개인별 평소 말투 기준선이 이 기능의 핵심이다.** 특정 단어를 절대 기준으로 잡지 않고
그 사람의 평소 대비 변화량(마침표율·호칭·길이·ㅋ 개수·이모지)으로 판정한다.
`data/speaker_profiles.json` 시드 + `worker/profile.py`.

출력 4종이 화면과 그대로 대응한다:

| 필드 | 내용 | 예시 |
| --- | --- | --- |
| `situationDiagnosis` | 상황 진단 (감정 알아봄) | "지금 감정이 조금 올라와 있는 것 같아요" |
| `guideMessage` | 고정 안내 문구 | "대신 이렇게 상대방에게 말해보세요." |
| `alternativeSentence` | 대체 문장 (나 전달법) | "오늘 못 온다니까 좀 서운했어, 다음엔 미리 말해줘" |
| `correctionReason` | 그렇게 읽히는 이유 | "'맨날'은 그동안 전부로 들릴 수 있어요" |

**대체 문장은 1~2문장(45자 이내)으로 짧게.** 복사 버튼이 없어서 보고 직접 타이핑해야
하기 때문이다. 나 전달법 4요소를 다 넣으면 외워서 칠 수 없는 길이가 된다.
화면은 73자까지 받지만(아래) **그건 상한이지 목표가 아니다.**

**대체 문장은 말투만 바꾼다. 뜻을 바꾸지 않는다.** 사용자가 그대로 보낼 문장이라
원문에 없던 선호·조건·요구를 넣으면 **의사가 바뀐 채로 상대에게 전달된다.**
실측 실패: `홍대도 좋아` → "홍대도 괜찮은데 난 강남이 더 좋아" (동의를 비교로 바꿈).

**거친 어휘와 호칭은 대체 문장에 남기지 않는다.** 주어만 '나'로 바꾸고 단어를 그대로 두면
교정이 안 된다 — 실측 실패 두 건이 그것이다.

| 원문 | ❌ 실측 출력 | ✅ 지금 |
| --- | --- | --- |
| `짜증나` | "나 지금 좀 **짜증**나서 그래" | "나 지금 좀 **속상**해서 그래" |
| `야 너는 맨날 그런 식이야` | "**야,** 오늘 또 못 온다니까 좀 서운해" | "오늘 그런 식이라서 좀 속상했어" |

**감정을 지우는 게 아니라 세기와 색만 낮춘다** — 짜증 → 속상. 감정을 빼면 나 전달법이
성립하지 않고 하지 않은 말("괜찮아")을 시키게 된다. `화나`·`서운`·`속상` 은 정당한 감정
표현이라 안 막는다. 호칭은 **평소 대비**로 본다 — 늘 "야"라고 부르는 사이면 거친 게 아니다.

`tone.harsh_in()` 이 검사하고 걸리면 **1회 재생성**한다. 그래도 남으면 트레이스에 남기고
**그대로 내보낸다** — 금지어와 달리 절대 제약이 아니고, 조금 거칠어도 원문(공격 표현)보다는
낫기 때문이다. 자수 한도를 잘라서라도 내보내는 것과 같은 판단이다.

**대체 문장이 원문과 사실상 같으면 내보내지 않는다** (`tone.same_message()`, 2026-08-20).
실측: "너 어제 공원에서 술 마신 거 누구야" → "어제 공원에서 술 마신 사람 누구야?" —
바뀐 게 없는 "교정" 카드는 기능 고장으로 보인다. 공백·문장부호·ㅋㅎ 제거 후 유사도
`SAME_RATIO=0.75` 이상이면 1회 재생성, 그래도 같으면 **버린다** — 거친 어휘·근거 조작과
달리 남는 가치가 0이라서다 (바꿀 게 없었다 = 침묵이 올바른 출력). 임계값은 실측 11쌍
캘리브레이션 — 좋은 교정은 전부 0.40 이하, 가짜는 0.86 이상이었다.

**이유가 원문에 없는 특징을 주장하면 안 된다** (`tone.ungrounded_in()`, 2026-08-20).
마침표 없는 메시지에 "마침표로 끝나 단호하게"가 나갔다(사용자 발견) — 프롬프트의 "평소
말투 기준선" 설명을 이번 메시지의 근거로 끌어다 쓴 것. 프롬프트에 근거 실재성 규칙을
넣고, 코드가 검증 가능한 주장(마침표·물음표·느낌표 실재, 따옴표 인용 실재)을 재검해
걸리면 1회 재생성한다 — `harsh_in` 과 같은 취급(버리지는 않음). ㅋ·이모지 **부재** 언급은
게이트 신호가 부재 기반이라 정당한 근거로 통과시킨다.

**애매하면 개입하지 않는다.** "와 미친 ㅋㅋ" 같은 장난을 갈등으로 오인하는 게 최대 리스크다.
`fixtures/case8_banter.json`(장난)과 `fixtures/case12_agree.json`(짧은 동의)이 검증한다.

### 2. 데이트 코스 추천 (`DATE_RECOMMENDATION`)

데이트 계획 의도가 감지되면 **두 사람이 과거에 남긴 발화를 최우선 근거로** 코스를 제시한다.

트리거 4종: 명시적 계획 질문 / 일정 확정 발화 / 장소·음식 카테고리 언급 / 이전 기억 재언급

**장소는 카카오 로컬 API 로만 가져온다.** LLM 은 검색어와 이유만 만든다 (위 아키텍처 참조).

**코스는 밥 → 구경 → 카페 세 자리로 고정이다** (`date_course.SLOTS`).

| 자리 | 허용 카테고리 |
| --- | --- |
| 밥 | `RESTAURANT` |
| 구경 | `CULTURE` · `ATTRACTION` · `SHOP` · `ACTIVITY` |
| 카페 | `CAFE` |

**검색어에 업종이 담겨 있어도 카카오가 그 업종으로 준다는 보장이 없다.** 검색어만 순서대로
받아 채웠더니 `브런치` 검색이 카페를 물어와 **카페가 두 곳인 코스**가 나왔다(`case9` 실측).
그래서 자리마다 카테고리를 강제하고, 안 맞는 결과는 후보에서 뺀다.

검색어는 **자리마다 2개씩** LLM 에게 받는다 (앞이 0건이면 뒤를 쓴다).
**파이썬이 대체 검색어를 지어내지 않는다** — 그러면 "무엇을 찾을지는 LLM" 역할 구분이
깨진다. 한 자리라도 못 채우면 미발동한다.

**대화에서 하고 싶다고 말한 활동은 그 자리의 첫 칸에 온다.** 두 번째 칸은 앞이 0건일
때만 쓰이므로, 뒤에 두면 사용자가 말한 것이 화면에 안 나온다. "영화나 볼까"에
`sight_queries=["전시","영화관"]` 을 써서 **전시관이 잡히고 추천 이유만 영화를 말한** 실측
실패가 있었다.

> ⚠️ **지역을 못 잡으면 미발동한다** (`router.py`). 프롬프트에 이미 "지역 단서가 없으면
> `should_recommend=false`" 라고 적혀 있지만 **LLM 이 안 지킨다.** `region=none` 인 채로
> 발동해서 카카오 전국 1위가 코스로 나갔다 — **서울 중구**, **충남 천안**이 실제 출력이다.
> 코스 안의 동선은 이어지지만(첫 장소가 앵커) 그 동선이 커플과 아무 상관이 없다.
> 프롬프트로 안 되는 것을 코드가 막는 자리다 — 자리별 카테고리 강제·자수 한도와 같은 패턴.

**격앙 상태면 보류한다** (`router.SUPPRESS_DATE_WHEN_ESCALATED`, 2026-08-20). 배포 실측에서
"자기 진짜 개같아" 3초 뒤에 코스가 저장됐다 — 병합 감정 라벨이 `ESCALATED` 면 결과를 담기
직전에 데이트만 내려놓는다 (상태는 같은 요청에서 병렬로 나와 있어 지연 0). **ACCUMULATED
(서운)는 안 막는다** — 서운할 때의 데이트 제안은 화해 시도일 수 있다 (사용자 결정).
시연 강제 트리거는 이 보류를 무시한다. `fixtures/case20_date_fight.json` 이 이 케이스다.

**같은 화제엔 코스 하나만** (2026-08-21 통합 시연 실측). 게이트가 방금 발화가 아니라
활성 세그먼트 전체를 봐서, "데이트할까?" 뒤에는 화제가 이어지는 동안 **모든 메시지가
코스를 다시 태웠다** — "사랑해 그날 봐"에서도 새 코스가 나갔고, 최근 장소 제외
(`recent_keys`)와 겹쳐 재발동마다 **다른 가게**로 바뀌었다. 유튜브와 같은 세그먼트
억제를 걸었다: 마지막 코스의 `createdAt` 이 활성 세그먼트 시작 뒤면 보류, 화제가
바뀌면(새 세그먼트) 바로 풀린다. 근거는 `recentResults[].createdAt` 이라 워커 무상태
원칙 그대로고, 서버가 안 실어주면 억제도 안 걸린다. 시연 강제 트리거는 다른 억제처럼
이것도 건너뛴다. 유튜브와 달리 시간 백스톱은 없다 — 카카오 쿼터는 넉넉하고, 세그먼트가
갈리면 게이트 자체가 데이트 낱말을 잃어 조용해진다. `fixtures/case21_date_refire.json`
이 이 케이스다.

**"데이트할까/하자/갈까"가 게이트에 빠져 있었다** (2026-08-20 수정). 가장 정석적인 제안
문장인데 `_PLAN_RE` 는 "데이트 어디/뭐/코스"만 봤다 — 배포에선 데모 트리거('데이트' 낱말)가
대신 잡아줘서 안 드러났고, **데모 모드를 끄면 "성수에서 데이트할까?"로 코스가 안 뜨는
상태였다.** 데모 트리거가 자연 게이트의 구멍을 가릴 수 있다는 사례다.

명세의 적합도 스코어링 5개 중 ①과거 기억 ②현재 스트림 ⑤취향만 구현했다.
③영업시간은 카카오가 주지 않고(크롤링 영역), ④날씨·예산은 데이터 소스가 없다.

`recommendationReason` 이 이 기능의 핵심이다 — "지난주에 마라탕 땡긴다고 하셨던 거
기억하고 골라봤어요". **근거에 없는 발화를 지어내면 안 된다.**

### 3. 유튜브 영상 추천 (`YOUTUBE_RECOMMENDATION`)

**고민(냉각기) 갈래만 탄다** — 싸우고 서먹할 때 영상 1개. 노출은 LLM 이 정한다(개별/공통),
안내 문구는 `YOUTUBE_GUIDE`. 룰 프리필터(`check_concern_gate`, 고민 어휘) → LLM 분류 →
검색 → 댓글 검증 → 1개 선정.

> ⛔ **화제 갈래(일상 대화 영상 추천)는 비활성화됐다 (2026-08-19 사용자 결정).**
> 명세(`spec.md` 3장)의 "관계 고민 신호 **또는 공통 관심 주제**" 중 뒤쪽을 끊은 것이다 —
> **명세 원문은 아직 두 갈래라 PM 문서와 어긋난 상태**고, PM 이 명세를 갱신해야 한다.
> 구현(`youtube.classify_topic`·`check_topic_gate`·`yt_topic.md`·`yt_pick_topic.md`)은
> 검증까지 끝난 채 남아 있고 `router.YoutubeCandidate.build()` 에서만 끊었다.
> '유튜브' 낱말 시연 강제 트리거도 화제 갈래를 태우는 것이라 함께 내렸다.
> 되살리기: build() 에서 고민 게이트 불통과 시 `_topic()` 복원 (git 01e8fc7 이전).

**제목·썸네일만으로 고르지 않는다. 베스트 댓글 3개를 함께 읽는다.** 연애 유튜브는 제목이
자극적이고 내용이 딴판인 경우가 흔해서, 화해하고 싶은 사람에게 "이런 남자는 걸러라"
영상을 던지게 된다. 댓글이 영상의 실제 결론을 알려준다.

**침묵이 기본값이다.** 후보 전원이 부적합하면 아무것도 내보내지 않는다.
댓글이 꺼진 영상은 맥락 검증을 못 하므로 후보에서 제외한다.

**영상 내용에 책임지는 표현을 쓰지 않는다.** "이렇게 하면 해결돼요"(X) /
"비슷한 상황을 다룬 영상이에요"(O). 우리가 만든 영상이 아니라 효과를 약속할 수 없다.

`router.py`의 `SUPPRESS_YOUTUBE_WHEN_TONE` — 말투 교정이 발동한 요청에서는 보류한다.
방금 공격적인 메시지를 보낸 사람에게 교정 제안과 영상이 동시에 뜨면 훈계가 된다.

**같은 화제엔 영상 하나만 — 세그먼트 기준 억제 + 시간 백스톱** (2026-08-19).
백엔드가 **메시지 1건마다** 워커를 부르기로 확정돼서(2026-08-18), 억제가 없으면 같은
화제가 이어지는 동안 메시지마다 검색이 나가고 하루 쿼터가 대화 한 번에 마른다.
마지막 영상의 `createdAt` 이 **활성 세그먼트 시작 뒤면** 지금 화제에 이미 답한 것이라
보류하고, 화제가 바뀌면(새 세그먼트) 바로 풀린다. 처음엔 30분 고정 쿨다운이었는데
화제를 못 봐서 양쪽으로 틀렸다 — 새 화제엔 30분 안이라도 떠야 하고, 같은 화제엔 30분
지나도 두 번 뜨면 안 된다. `YOUTUBE_COOLDOWN_MIN`(기본 5분)은 백스톱으로만 남았다 —
세그먼트 경계가 LLM 산출이라 흔들릴 때의 검색 폭주를 분당 상한으로 누른다.
근거는 서버가 실어주는 `recentResults[].createdAt` 이라 **워커가 상태를 갖지 않는다는
원칙을 깨지 않는다.** 그 필드가 없으면 억제도 안 걸린다.
억제를 게이트보다 **먼저** 보는 이유는 뒤에 두면 LLM 분류를 이미 부른 뒤라서다.
시연 리허설을 연달아 돌리려면 `KAKAPO_YOUTUBE_COOLDOWN_MIN=0` — 둘 다 꺼진다.

### 화면 글자 수 한도 (디자인 실측, 2026-08-17)

**넘으면 화면에서 잘린다.** 잘린 문장은 안 읽히는 게 아니라 뜻이 바뀐다 —
근거가 사라진 채 "…들릴 수 있어" 만 남는다. 띄어쓰기 포함 `len()` 기준이다.

| 기능 | 필드 | 한도 |
| --- | --- | --- |
| 감정 상태 조정 | `situationDiagnosis` | 35자 *(디자인 미지정 — 워커 기준)* |
| | `alternativeSentence` | **73자** (목표는 45자 — 위 참조) |
| | `correctionReason` | **52자** |
| 데이트 코스 | `recommendationReason` | **54자** (한 줄 27자 × 두 줄) |
| 유튜브 | `recommendationReason` | **50자** |
| 실 상태 표현 | `stateText` | **10자** |

**프롬프트에 자수를 적는 것만으로는 안 지켜진다.** 실측에서 넘는 출력이 계속 나왔다.
`worker/limits.py` 가 검사하고, 넘으면 **1회 재생성 → 그래도 넘으면 어절 경계에서 자른다.**
금지어 필터와 같은 구조인데 **마지막 처리가 다르다** — 금지어는 절대 제약이라 버리지만
길이는 조금 넘었다고 기능을 통째로 미발동시킬 이유가 없다.

재생성은 **문구 단계만** 다시 돈다. 데이트는 카카오 검색을, 유튜브는 검색(쿼터 100 units)을
다시 부르지 않는다.

> ⚠️ **워커 출력이 전부 화면에 들어가는 게 아니다.** 한도를 받은 필드만 `limits.MAX` 에
> 넣는다. 유튜브 영상 제목·채널명은 외부 값이라 워커가 줄이지 않는다 — 프론트가 말줄임한다.

### 파킹됨 — 대화 소재 제시

우선순위에서 밀려 `parked/` 로 뺐다. 코드는 검증까지 끝난 상태로 보관돼 있고
복구 방법은 `parked/README.md` 에 있다. **RAG(`retrieve.py`)와 기억 추출(`extract.py`)은
파킹하지 않았다** — 데이트 코스가 그대로 쓴다.

---

## 기억 저장소 + RAG

데이트 코스 추천의 근거가 되는 저장소. 없으면 "왜 이 장소인지"를 말할 수 없어서
그냥 아무 맛집이나 추천하는 서비스가 된다.

**검색은 RAG로 한다.** 단순 최신순 조회가 아니라 현재 대화 맥락과 의미적으로 유사한
기억을 찾는다.

```
대화: "아 배고파... 뭐 먹지"

최신순 조회  → "작년에 갔던 방탈출"              (맥락 무관)
유사도 검색  → "먹고 싶다던 연남동 크림파스타집"   (맥락 연결)
```

이 차이가 "AI가 진짜 듣고 있다"는 느낌을 만든다. 시연에서 가장 잘 먹히는 지점이다.

기억이 27건 규모라 `InMemoryVectorStore`로 충분하다. pgvector나 Chroma를 띄우지 않는다.
프로세스 시작 시 `data/memories.json`을 한 번 인덱싱한다.

### 종류

| kind | 의미 | 예시 |
| --- | --- | --- |
| `place` | 함께 간 장소 | 성수동 카페 오르에르 |
| `activity` | 함께 한 활동 | 같이 간 성수 방탈출 |
| `promise` | 지키지 못한 약속 | 같이 가기로 한 사진 전시 |
| `wish` | 저장만 하고 안 쓴 위시 | 가보고 싶다던 마라탕 맛집 |
| `interest` | 두 사람의 관심사 | 좋아하는 것, 취향 |
| `schedule` | 만나기로 한 시점 | 이번주 토요일 낮 약속 |

`schedule` 은 데이트 코스 명세의 "일정"이다. 시점이 핵심이라 `promise` 와 다르다.

`used_at`이 "미소환 우선"과 "30일 내 중복 금지"를 동시에 처리한다.

### 기억 추출

별도 배치 파이프라인을 만들지 않는다. **분석 요청마다 `extract.py` 가 한 번 같이 돈다.**
**응답을 기다리게 하지는 않는다** (백그라운드, 2026-09-13) — 이번 요청의 데이트 코스는
대화 원문을 프롬프트로 직접 받아서 방금 "마라탕 땡긴다"고 한 발화는 저장소를 안 거쳐도
반영되고, 저장은 **다음 요청**을 위한 것이다. CLI·devui 는 `wait_background=True` 로
기다려서 추출 결과를 보여준다.

**원문에 실제로 존재하는 인용만 저장한다.** 지어낸 기억은 나중에 추천 이유로 화면에
그대로 나간다 — "3주 전에 성수 가보고 싶다고 하셨어요"라고 했는데 그런 말을 한 적이
없으면 서비스 신뢰가 통째로 무너진다.

**시드 데이터가 필수다.** 실시간 추출만으로는 데모 시작 시점에 기억이 0건이다.
현재 시드는 27건 (place 6 / activity 6 / promise 5 / wish 5 / interest 5).

---

## 시연 대비

해커톤 목표는 심사위원에게 "진짜 웃긴 팀, 잘되겠다"는 반응을 얻는 것이다.

- 3분 데모에서 보여줄 수 없는 기능에 시간을 쓰지 않는다
- **반복 시연은 `--no-persist`로 돌린다.** 안 그러면 소환된 기억에 `used_at`이 찍혀서
  두 번째부터 다른 결과가 나온다
- **시연 강제 트리거** — `KAKAPO_DEMO_TRIGGERS=1` 이면 방금 발화의 '데이트' 낱말로
  게이트·억제·LLM 보류를 건너뛰고 무조건 발동한다 (`router.demo_hit`). 기본 꺼짐.
  절대 제약(금지어 필터·실장소/실영상·자수 한도)은 데모에서도 그대로다.
  ('유튜브' 트리거는 화제 갈래 비활성화와 함께 내렸다 — 냉각기 영상은 자연 발동만)

### 시연 추천 순서

| 케이스 | 보여주는 것 |
| --- | --- |
| `case9_date` | RAG + 실재 장소 — 기억을 근거로 진짜 가게와 링크를 가져온다 |
| `case7_tone` | 갈등 중재 — 공격 표현 → 보낸 사람에게만 대체 문장 |
| `case8_banter` | 안전장치 — 똑같이 비속 표현인데 장난이면 아무것도 안 뜬다 |
| `case10_concern` | 유튜브 — 댓글까지 읽고 고른다 |

`case7`과 `case8`을 붙여서 보여주는 게 가장 설득력 있다. 감지가 어휘 필터가 아니라
맥락 판정이라는 걸 한 번에 보여준다.

`--json` 을 붙이면 백엔드에 나가는 규격서 응답이 그대로 보인다. 서버 담당자와 얘기할 때 쓴다.

---

## 작업 원칙

- 명세와 코드가 어긋나면 **명세가 맞다.** 명세를 바꿔야 한다고 판단되면 코드를 임의로
  고치지 말고 사용자에게 먼저 알린다
- **서버 연동 규격은 팀 합의 사항이다.** 한쪽만 바꾸면 상대 서버가 깨진다.
  워커 쪽 의견은 `docs/server-handoff.md` 에 적고 협의한다
- **LLM 에게 실재하는 것의 이름·링크를 만들게 하지 않는다** (장소, 영상, 인용문)
- **문서 이름은 주제로만 짓는다. 버전 번호를 미리 붙이지 않는다.**

  옛 세대를 실제로 남겨야 할 때 — 기존 문서를 크게 뜯어고치는데 이전 결정 근거를 보존해야
  할 때 — **그때** 옛 파일에 `-v<n>` 을 붙여 밀어내고 새 파일이 주제 이름을 갖는다.

  전에는 명세·설계에 처음부터 번호를 붙였는데 **정보를 담지 못했다.**
  `contract-v1` · `spec-v2` · `segmentation-v3` · `state-display-v4` 가 한 문서의 세대처럼
  읽히는데 실제로는 전부 다른 주제였고 각각 세대가 하나뿐이었다. `contract` 는 v2 를
  폐기하고 **v1 으로 거꾸로 가기까지 했다.** 붙은 숫자가 "몇 번째 세대"도 "어느 게 최신"도
  안 알려주면 그건 이름이 아니라 잡음이다 (2026-08-18 정리).

  **전달본은 그때도 번호를 안 붙인다.** 받는 쪽이 "몇 번 문서가 최신이지"를 고민하게
  만들면 안 되므로 같은 파일을 갱신한다.
- 확실하지 않으면 추측해서 진행하지 말고 질문한다
