langchain>=1.3
langchain-openai>=1.5
pydantic>=2
python-dotenv

# numpy 는 기억 RAG(InMemoryVectorStore) 전용이었다 — 저장소 파킹(2026-09-13)과 함께 뺐다.
# 되살리면 다시 넣는다 (`parked/memory/README` 참조).

# 카카오 로컬 API (장소 검색) / YouTube Data API v3 (영상 + 베스트 댓글)
httpx

# 개발 도구 전용 — 워커 런타임에는 필요 없어서 여기 넣지 않는다.
# tools/build_eval_set.py (평가셋 빌드) 를 돌릴 때만:  pip install pyarrow

# HTTP 서버 (`worker/api.py`). 판정 로직은 갖지 않고 analyze() 를 물리기만 한다.
fastapi>=0.115
uvicorn>=0.30
